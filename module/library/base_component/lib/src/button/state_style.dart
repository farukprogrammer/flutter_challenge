import 'package:flutter/material.dart';

class StateStyle {
  final Color enabled;
  final Color disabled;
  final Color pressed;

  const StateStyle(
      {required this.enabled, required this.disabled, required this.pressed});
}
